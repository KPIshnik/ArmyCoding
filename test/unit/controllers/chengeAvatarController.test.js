describe("testing chengeAvatarController", () => {
  //when avatar chenged succesfuly should send status 200 and message:
  //"avatar for {username} updated"
  //
  //when old avatar deleted should create msg "avatar for {username} updated"
  //
  //when err should send status 200 and msg "chenging avatar failed"
  //
  // when no username passed  status 400 msg"username required"
});
